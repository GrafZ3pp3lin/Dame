package test;

import Model.Color;
import Model.Player;/*
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class DameTest {

    @Test
    public void testStonesLocation() {
        Player p = new Player(Color.BLACK, "", 8);
        //Stein 1 (0|0)
        assertEquals(0, p.getStones()[0].getIndexX());
        assertEquals(0, p.getStones()[0].getIndexY());
        //Stein 12 (6|2)
        assertEquals(6, p.getStones()[11].getIndexX());
        assertEquals(2, p.getStones()[11].getIndexY());
        //Gesamtmenge
        assertEquals(12, p.getStones().length);
    }
}*/
